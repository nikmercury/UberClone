import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    padding: 10,
    height: '100%',
  },
  textInput: {
    padding: 5,
    backgroundColor: 'lightgrey',
    marginVertical: 5,
  },
});
